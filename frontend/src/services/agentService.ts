// Agent service
export const agentService = {
  loadAgents: async () => {
    // Placeholder for loading agents
    return Promise.resolve([]);
  },
  getAgents: async () => {
    // Placeholder for getting agents
    return Promise.resolve([]);
  },
  createAgent: async (agent: any) => {
    // Placeholder for creating agent
    return Promise.resolve(agent);
  },
  updateAgent: async (id: string, agent: any) => {
    // Placeholder for updating agent
    return Promise.resolve(agent);
  },
  deleteAgent: async (id: string) => {
    // Placeholder for deleting agent
    return Promise.resolve();
  }
};
